import LoginButton from '@/components/login-button';

export const metadata = {
  title: 'Home',
};

const HomePage = () => {
  return (
    <>
      <LoginButton />
    </>
  );
};

export default HomePage;

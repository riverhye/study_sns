'use client';

import SignIn from "@/components/sign/SignIn";

export const metadata = {
  title: 'Home',
};

const HomePage = () => {
  return (
    <>
      <SignIn />
    </>
  );
};

export default HomePage;
